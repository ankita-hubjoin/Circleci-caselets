version: 2.1

jobs:
  build:
    working_directory: ~/circleci-project-setup
    docker:
      - image: "circleci/python:3.9.0"
    steps:
      - checkout
      - run: export MPLBACKEND='Agg'
      - run: python3 billing_system.py
  test1:
    working_directory: ~/circleci-project-setup
    docker:
      - image: "circleci/python:3.9.0"
    steps:
      - checkout
      - run: python3 test_billing_system_1.py
  test2:
    working_directory: ~/circleci-project-setup
    docker:
      - image: "circleci/python:3.9.0"
    steps:
      - checkout
      - run: python3 test_billing_system_2.py

  get_artifact:
    working_directory: ~/circleci-project-setup
    steps:
      - run:
          name: Creating Dummy Artifacts
          command: |
            echo "my artifact file" > /circleci-project-setup/art-1;
            mkdir /circleci-project-setup/artifacts;
            echo "my artifact files in a dir" > /circleci-project-setup/artifacts/art-2;
      - store_artifacts:
          path: /circleci-project-setup/art-1
          destination: artifact-file
      - store_artifacts:
          path: /circleci-project-setup/artifacts

workflows:
  build_and_test:
    jobs:
      - build
      - test1:
          requires:
            - build
      - test2:
          requires:
            - build
      - get_artifact:
          requires:
            - test1
            - test2
